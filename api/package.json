{
  "name": "weather-swa-api",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "func start",
    "dev": "npm run build -- --watch",
    "test": "vitest run --reporter=dot",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@azure/functions": "^4.5.0",
    "@types/node": "^24.7.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
